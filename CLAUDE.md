You can find a similar project here: `C:\Dev\ML\caped`
- The Godot UI is in the App folder.
- You can take ideas and pieces of code from there as needed. 
- There is also some asset pre-processing in that application, this may be useful for icons and other texture assets.
- Asset pre-processing requires Python, which has already been prepared in this project. 
- Add Python packages as needed. Use `uv` as the Python package manager.
